# matrix

# Библиотека для работы с матрицами на C

Полноценная библиотека на языке C для выполнения операций с матрицами, разработанная в рамках учебного проекта. Реализует создание, арифметические операции, преобразования и продвинутые функции линейной алгебры с корректным управлением памятью и обработкой ошибок.

---

## 📋 Содержание

- [Структура проекта](#-структура-проекта)
- [Возможности](#-возможности)
- [Требования](#-требования)
- [Установка и сборка](#-установка-и-сборка)
- [Справочник по API](#-справочник-по-api)
- [Коды ошибок](#-коды-ошибок)
- [Тестирование](#-тестирование)
- [Покрытие кода](#-покрытие-кода)

---

## 📁 Структура проекта

    matrix-lib/
├── Makefile
├── README.md
├── matrix.h
├── matrix.c
└── test/
    └── test_matrix.c

## ✨ Возможности

✅ **Управление памятью**
- Динамическое выделение через `calloc()` с обнулением ячеек
- Безопасное освобождение памяти через `remove_matrix()` для предотвращения утечек

✅ **Базовые операции**
- Создание и удаление матриц
- Сравнение матриц с учётом погрешности (`EPSILON = 1e-6`)
- Сложение, вычитание, умножение на скаляр
- Умножение матриц (скалярное произведение)

✅ **Продвинутая линейная алгебра**
- Транспонирование матрицы
- Вычисление определителя (рекурсивно, для любых размеров)
- Матрица алгебраических дополнений (кофакторов)
- Обратная матрица (для невырожденных квадратных матриц)

✅ **Надёжная обработка ошибок**
- Чёткие коды возврата: успех, общая ошибка, ошибка вычислений
- Проверка на `NULL` и валидация размеров на каждом шаге

✅ **Тестирование и качество кода**
- Модульные тесты с использованием фреймворка Check
- Отчёты о покрытии кода через `gcov`/`lcov`
- Строгие флаги компиляции: `-Wall -Wextra -Werror`

---


## Установка и сборка

1. Клонируйте репозиторий
   ```bash
   git clone git@github.com:your-username/matrix-lib.git
   cd matrix-lib

2. Соберите статическую библиотеку
   ```bash
   make all
# Результат: libmatrix.a

3. Запустите тесты
   ```bash
   make test

4. Очистка артефактов сборки
   ```bash
   make clean

5. Пересборка + тесты одной командой
   ```bash
   make rebuild

## Справочник по API
    Основная структура
    ```c
    typedef struct matrix_struct {
    double **matrix;  // Двумерный массив значений
    int rows;         // Количество строк
    int columns;      // Количество столбцов
    } matrix_t;

### 🔹 Функции создания и удаления

| Функция | Описание | Параметры | Возвращает |
|---------|----------|-----------|------------|
| `create_matrix` | Выделение памяти под матрицу | `int rows`, `int columns`, `matrix_t *result` | `OK` / `ERROR` |
| `remove_matrix` | Освобождение выделенной памяти | `matrix_t *A` | `void` |

### 🔹 Арифметические операции

| Функция | Описание | Параметры | Возвращает |
|---------|----------|-----------|------------|
| `sum_matrix` | Поэлементное сложение матриц | `matrix_t *A`, `matrix_t *B`, `matrix_t *result` | `OK` / `ERROR` / `CALC_ERROR` |
| `sub_matrix` | Поэлементное вычитание матриц | `matrix_t *A`, `matrix_t *B`, `matrix_t *result` | `OK` / `ERROR` / `CALC_ERROR` |
| `mult_number` | Умножение матрицы на скаляр | `matrix_t *A`, `double number`, `matrix_t *result` | `OK` / `ERROR` |
| `mult_matrix` | Умножение двух матриц | `matrix_t *A`, `matrix_t *B`, `matrix_t *result` | `OK` / `ERROR` / `CALC_ERROR` |

### 🔹 Продвинутые операции

| Функция | Описание | Параметры | Возвращает |
|---------|----------|-----------|------------|
| `transpose` | Транспонирование матрицы | `matrix_t *A`, `matrix_t *result` | `OK` / `ERROR` |
| `determinant` | Вычисление определителя | `matrix_t *A`, `double *result` | `OK` / `ERROR` / `CALC_ERROR` |
| `calc_complements` | Матрица алгебраических дополнений | `matrix_t *A`, `matrix_t *result` | `OK` / `ERROR` / `CALC_ERROR` |
| `inverse_matrix` | Вычисление обратной матрицы | `matrix_t *A`, `matrix_t *result` | `OK` / `ERROR` / `CALC_ERROR` |

### 🔹 Сравнение матриц

| Функция | Описание | Параметры | Возвращает |
|---------|----------|-----------|------------|
| `eq_matrix` | Сравнение с учётом погрешности | `matrix_t *A`, `matrix_t *B` | `SUCCESS` / `FAILURE` |



## Коды ошибок
    ```c

    #define OK 0           // Операция успешна
    #define ERROR 1        // Некорректные аргументы / ошибка выделения памяти
    #define CALC_ERROR 2   // Вычисление невозможно (несовпадение размеров, вырожденная матрица)

    #define SUCCESS 1      // Для сравнения: матрицы равны
    #define FAILURE 0      // Для сравнения: матрицы различаются

    #define EPSILON 1e-6   // Допустимая погрешность при сравнении чисел с плавающей точкой

## Тестирование
    ```bash
    make test
# Выполняет: ./test_suite

# Структура тестов

Тесты находятся в файле test/test_matrix.c и используют фреймворк Check.


## Покрытие кода
    ```bash    
    make gcov_report
    # Отчёт будет создан в папке: report/index.html

